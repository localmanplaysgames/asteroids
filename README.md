# asteroids

note to self - "python3 -m venv venv" to create virtual enviroment on new machine. "source venv/bin/activate" to activate it. "pip install -r requirements.txt" to pull down requirements.

don't submit any boot.dev stuff without activating the virtual environment